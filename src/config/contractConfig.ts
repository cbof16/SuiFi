// Challenge types
export const CHALLENGE_TYPES = {
  STEP_SHOWDOWN: '1',
  STAKE_FLEX: '2',
  SPEED_STEPS: '3',
  SYNC_SHOWDOWN: '4'
};

// Challenge type enum to match usage in the create page
export enum CHALLENGE_TYPE_ENUM {
  STEPS = 1,
  RUNNING = 2,
  CYCLING = 3,
  SWIMMING = 4
}

// UI friendly challenge type names 